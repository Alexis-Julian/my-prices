import { View, Text } from "react-native";
import ListCard from "../../../components/CardList";

function Catalogue() {
	return (
		<View style={{ flex: 1 }}>
			<ListCard />
		</View>
	);
}

export default Catalogue;
